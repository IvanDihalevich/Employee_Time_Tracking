# 🚀 Швидкий запуск проекту

## Варіант 1: Автоматичний запуск (рекомендовано)

Подвійний клік на файл **`start.bat`** або в терміналі:

```bash
start.bat
```

Скрипт автоматично:
- ✅ Перевірить наявність конфігураційних файлів
- ✅ Запустить Backend сервер (порт 5000)
- ✅ Запустить Frontend сервер (порт 3000)
- ✅ Відкриє два окремі вікна терміналу

---

## Варіант 2: Ручний запуск

### Крок 1: Запуск Backend

Відкрийте термінал і виконайте:

```bash
cd backend
npm run dev
```

Backend буде доступний на: **http://localhost:5000**

### Крок 2: Запуск Frontend

Відкрийте **новий термінал** і виконайте:

```bash
cd frontend
npm run dev
```

Frontend буде доступний на: **http://localhost:3000**

---

## ⚠️ Перший запуск

Якщо запускаєте вперше, спочатку виконайте:

### 1. Встановлення залежностей

```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

### 2. Налаштування бази даних

```bash
cd backend
npm run prisma:generate
npm run prisma:push
```

### 3. Перевірка конфігурації

Переконайтеся, що існують файли:
- ✅ `backend/.env` - з налаштуваннями бази даних
- ✅ `frontend/.env.local` - з URL backend API

---

## 🌐 Доступ до додатку

Після запуску відкрийте браузер:
- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:5000/api

---

## 🛑 Зупинка серверів

- Якщо використовували `start.bat` - просто закрийте обидва вікна терміналу
- Якщо запускали вручну - натисніть `Ctrl + C` в кожному терміналі

---

## ❓ Проблеми?

### Помилка: "Cannot find module"
```bash
cd backend
npm install

cd ../frontend
npm install
```

### Помилка підключення до бази даних
Перевірте `backend/.env` - чи правильний `DATABASE_URL`

### Порт вже зайнятий
Закрийте інші програми, що використовують порти 3000 або 5000

---

**Готово! Проект запущено! 🎉**

