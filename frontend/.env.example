# Для Vercel: додай у Project → Settings → Environment Variables
# Локально скопіюй у .env.local

# Без слеша в кінці. Має закінчуватися на /api (як у lib/api.ts).
NEXT_PUBLIC_API_URL=http://localhost:5000/api
